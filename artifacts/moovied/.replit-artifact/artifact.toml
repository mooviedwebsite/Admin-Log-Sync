kind = "web"
previewPath = "/"
title = "MOOVIED"
version = "1.0.0"
id = "artifacts/moovied"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22224

[services.development]
run = "pnpm --filter @workspace/moovied run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/moovied", "run", "build" ]
publicDir = "artifacts/moovied/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22224"
BASE_PATH = "/"
